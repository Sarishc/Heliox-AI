🚀 Heliox has been deployed!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if .Values.ingress.enabled }}
ACCESS YOUR DEPLOYMENT
  https://{{ (index .Values.ingress.hosts 0).host }}
{{- else }}
ACCESS YOUR DEPLOYMENT (no Ingress configured)
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "heliox.fullname" . }}-api 8000:8000
  Then visit: http://localhost:8000
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CHECK POD STATUS
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

VIEW API LOGS
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=api -f

VIEW WORKER LOGS
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=worker -f

CHECK MIGRATION JOB
  kubectl get jobs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if .Values.agent.enabled }}
GPU AGENT
  The Heliox agent DaemonSet is running on nodes with label:
    accelerator=true

  To verify agent connectivity:
    kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=agent -f
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

UPGRADE
  helm upgrade {{ .Release.Name }} heliox/heliox -n {{ .Release.Namespace }} -f values-production.yaml

UNINSTALL
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Documentation: https://heliox.ai/docs/self-hosted
Support: engineering@heliox.ai
